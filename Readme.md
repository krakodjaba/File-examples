## 📄 Единый учебный README.md (на русском)

---

# Курс: Работа с файлами в OSINT

Этот репозиторий содержит практические примеры на Python для работы с форматами файлов, которые часто встречаются при OSINT-расследованиях. Каждый модуль самодостаточен, содержит пояснения и может использоваться как для обучения, так и для реальных задач.

---

## 📁 Структура

- **`new/`** — все учебные скрипты.
- Тестовые данные создаются автоматически почти в каждом скрипте.

---

## 📘 Модули

### 1. `blockchain_examples.py`
**Назначение в OSINT:**  
Анализ транзакций криптовалют, построение графов адресов, выявление крупных переводов.

**Функции:**
- `parse_transactions()` — загрузка JSON с транзакциями.
- `analyze_addresses()` — извлечение уникальных адресов.
- `calculate_transaction_volume()` — подсчёт общего объёма.
- `find_high_value_transactions()` — поиск транзакций выше порога.
- `export_to_graphml()` — экспорт в GraphML для Gephi.
- `plot_transaction_graph()` — визуализация графа.

**Как использовать:**
```bash
python new/blockchain_examples.py
# Создаёт: blockchain_data.json, transaction_graph.png, transactions.graphml
```

---

### 2. `pcap_examples.py`
**Назначение в OSINT:**  
Анализ PCAP-файлов, извлечение IP-адресов, фильтрация по протоколам, экспорт метаданных.

**Функции:**
- `read_pcap()` — чтение PCAP с обработкой ошибок.
- `extract_ips()` — извлечение IP-адресов.
- `filter_by_protocol()` — фильтрация по TCP/UDP.
- `export_to_csv()` — экспорт в CSV.
- `analyze_with_tshark()` — анализ через tshark (если установлен).

**Как использовать:**
```bash
python new/pcap_examples.py
# Создаёт: test_data.pcap, pcap_analysis.csv
```

---

### 3. `pdf_examples.py`
**Назначение в OSINT:**  
Извлечение текста и метаданных из PDF, OCR для сканов, анализ структуры.

**Функции:**
- `extract_pdf_text()` — извлечение текста.
- `extract_pdf_metadata()` — метаданные.
- `ocr_pdf_page()` — OCR для сканов.
- `analyze_pdf_structure()` — анализ структуры.
- `export_pdf_data()` — экспорт в CSV/JSON.
- `search_in_pdf()` — поиск по ключевому слову.

**Как использовать:**
```bash
python new/pdf_examples.py
# Создаёт: pdf_metadata.csv, pdf_metadata.json
```

---

### 4. `social_examples.py`
**Назначение в OSINT:**  
Парсинг JSON из соцсетей, анализ хэштегов, визуализация активности, выявление ботов.

**Функции:**
- `load_social_data()` — загрузка постов.
- `analyze_hashtags()` — подсчёт хэштегов.
- `visualize_activity()` — график активности.
- `detect_high_frequency_authors()` — выявление ботов.
- `export_to_gexf()` — экспорт графа для Gephi.

**Как использовать:**
```bash
python new/social_examples.py
# Создаёт: social_data.json, activity.png, social_graph.gexf
```

---

### 5. `email_examples.py`
**Назначение в OSINT:**  
Парсинг EML, извлечение вложений, проверка репутации отправителя, анализ заголовков.

**Функции:**
- `parse_eml()` — парсинг EML.
- `extract_headers()` — извлечение и декодирование заголовков.
- `save_attachments()` — сохранение вложений с хешами.
- `check_sender_reputation()` — проверка через API.
- `analyze_timestamps()` — анализ временных меток.
- `detect_suspicious_headers()` — выявление подозрительных заголовков.

**Как использовать:**
```bash
python new/email_examples.py
# Создаёт: test_email.eml, папку attachments/
```

---

### 6. `xml_examples.py`
**Назначение в OSINT:**  
Парсинг XML (RSS, конфиги), извлечение данных, валидация по XSD, поиск аномалий.

**Функции:**
- `read_xml()` — чтение XML.
- `write_xml()` — запись XML.
- `extract_xml_tags()` — извлечение по тегам.
- `xml_to_json()` — конвертация в JSON.
- `xml_to_csv()` — конвертация в CSV.
- `validate_xml_schema()` — валидация по XSD.
- `find_anomalies_in_xml()` — поиск пропущенных тегов.

**Как использовать:**
```bash
python new/xml_examples.py
# Создаёт: test_data.xml, test_data.json, test_data.csv
```

---

### 7. `yaml_examples.py`
**Назначение в OSINT:**  
Парсинг YAML (конфиги Maltego, SpiderFoot), фильтрация, валидация по схеме.

**Функции:**
- `read_yaml()` — чтение YAML.
- `write_yaml()` — запись YAML.
- `filter_yaml_by_key()` — фильтрация по ключу.
- `yaml_to_json()` — конвертация в JSON.
- `yaml_to_csv()` — конвертация в CSV.
- `validate_yaml_schema()` — валидация по JSON Schema.
- `parse_maltego_config()` — парсинг конфигов Maltego.

**Как использовать:**
```bash
python new/yaml_examples.py
# Создаёт: test_data.yaml, test_data.json, test_data.csv
```

---

### 8. `sqlite_examples.py`
**Назначение в OSINT:**  
Анализ дампов SQLite (логи приложений, чаты), поиск аномалий, экспорт данных.

**Функции:**
- `create_test_db()` — создание тестовой БД.
- `query_sqlite()` — выполнение SQL.
- `sqlite_to_csv()` — экспорт таблицы в CSV.
- `sqlite_to_json()` — экспорт таблицы в JSON.
- `analyze_db_structure()` — анализ структуры БД.
- `find_anomalies()` — поиск пустых значений.

**Как использовать:**
```bash
python new/sqlite_examples.py
# Создаёт: test_data.db, users.csv, users.json
```

---

### 9. `log_examples.py`
**Назначение в OSINT:**  
Парсинг логов веб-серверов, подсчёт статус-кодов, выявление подозрительных IP.

**Функции:**
- `parse_log_line()` — парсинг строки лога.
- `filter_logs_by_ip()` — фильтрация по IP.
- `count_status_codes()` — подсчёт статус-кодов.
- `detect_suspicious_ips()` — поиск IP с аномальной активностью.
- `logs_to_dataframe()` — конвертация в DataFrame.
- `export_logs_to_json()` — экспорт в JSON.

**Как использовать:**
```bash
python new/log_examples.py
# Создаёт: test_data.log, logs.json
```

---

### 10. `archive_examples.py`
**Назначение в OSINT:**  
Анализ архивов (ZIP/TAR), поиск в содержимом, выявление исполняемых файлов.

**Функции:**
- `list_archive_files()` — список файлов в архиве.
- `extract_file_from_archive()` — извлечение файла.
- `search_in_archive()` — поиск по содержимому.
- `analyze_archive_metadata()` — анализ метаданных.
- `detect_executable_files()` — поиск исполняемых файлов.
- `export_archive_data_to_csv()` — экспорт метаданных в CSV.

**Как использовать:**
```bash
python new/archive_examples.py
# Создаёт: test_data.zip, archive_metadata.csv
```

---

## 🛠️ Требования

### `requirements.txt`
```
pandas
matplotlib
networkx
requests
scapy
PyPDF2
pdfminer.six
pytesseract
pillow
pdf2image
lxml
PyYAML
jsonschema
```

**Установка:**
```bash
pip install -r requirements.txt
```

---

## 📂 Какие файлы с тестовыми данными нужно создать отдельно?

Список файлов, которые **не создаются** скриптами и требуются для запуска:

| Файл | Где используется | Создаётся скриптом? |
|------|------------------|---------------------|
| `example.pdf` | `pdf_examples.py` (OCR) | Нет |
| `example.pcap` | `pcap_examples.py` (анализ) | Нет |
| `example.eml` | `email_examples.py` (парсинг) | Нет |
| `blockchain_data.json` | `blockchain_examples.py` (транзакции) | Да (в скрипте) |
| `social_data.json` | `social_examples.py` (посты) | Да (в скрипте) |
| `test_data.xml` | `xml_examples.py` (XML) | Да (в скрипте) |
| `test_data.yaml` | `yaml_examples.py` (YAML) | Да (в скрипте) |
| `test_data.db` | `sqlite_examples.py` (SQLite) | Да (в скрипте) |
| `test_data.log` | `log_examples.py` (логи) | Да (в скрипте) |
| `test_data.zip` | `archive_examples.py` (архив) | Да (в скрипте) |

**Вывод:** Вам нужно создать отдельно:
- `example.pdf`
- `example.pcap`
- `example.eml`

---

## 🎯 Как использовать

1. Установите зависимости:
   ```bash
   pip install -r requirements.txt
   ```
2. Запустите любой модуль:
   ```bash
   python new/<имя_модуля>.py
   ```
3. Изучите созданные файлы (CSV, JSON, изображения) — они готовы для дальнейшего анализа.

---

## 📚 Советы по OSINT

- **Blockchain:** Используйте GraphML в Gephi для визуализации связей между адресами.
- **PCAP:** Сопоставляйте IP с угрозами из открытых источников.
- **PDF:** Всегда проверяйте метаданные на наличие автора и скрытой информации.
- **Social:** Ищите временные всплески активности и координируемые кампании.
- **Email:** Проверяйте отправителей через API и ищите признаки подмены заголовков.
- **XML/YAML:** Сравнивайте схемы с эталонными для выявления изменений.
- **SQLite:** Ищите удалённые записи и аномалии в логах.
- **Logs:** Коррелируйте подозрительные IP с внешними базами угроз.
- **Archives:** Остерегайтесь вложенных архивов и исполняемых файлов.

---

**Успешных расследований!**  
Если есть вопросы или предложения — создайте issue или отправьте PR.